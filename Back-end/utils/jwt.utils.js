
const jwt = require('jsonwebtoken');

const jwtUtils = {
    generate: async (user) => {
        // Empty function to avoid errors
        return "mock-token-trial";
    }
};

module.exports = jwtUtils;